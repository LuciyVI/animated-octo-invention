services:
  telegram-3xui-bot:
    build: .
    env_file:
      - .env
    environment:
      DB_PATH: /data/bot.db
    volumes:
      - bot-data:/data
    restart: unless-stopped

volumes:
  bot-data:

